# Dependencies
node_modules/

# Build outputs
dist/
build/
.turbo/

# Environment secrets (NEVER commit)
.env
.env.*
!.env.example

# Firebase Admin SDK service account keys (NEVER commit)
*firebase-adminsdk*.json
firebase-admin.json
serviceAccountKey.json

# VS Code
.vscode/

# Test coverage
coverage/

# Prisma
.prisma/
prisma/dev.db
prisma/dev.db-journal

# Logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db

# Keystore & signing secrets (NEVER commit)
keystore/
apps/web/android/keystore/
apps/web/android/key.properties
*.jks
*.keystore

# Firebase config (contains API key)
apps/web/android/app/google-services.json

# Cache Vite / TypeScript
*.tsbuildinfo
.vite/

# Generated sitemaps
apps/web/public/sitemap*.xml

# Build artifacts
kin-sell-*.apk
kin-sell-*.aab
build-log.txt
temp-apk-extract/
temp-apk-icon.png
temp-apk.zip
temp-icon-check.png
apps/web/dist.tar.gz

# Scripts with credentials
scripts/fix-env-firebase.py

# Local pitch-deck work artifacts (Edge browser profile, caches, history, cookies)
docs/pitch-deck-work/

# Local downloads / binaries (APK distribution placed at runtime)
/downloads/
apps/web/public/downloads/
*.apk

# Local SMTP / email diagnostic tooling (uses .env, not for prod)
scripts/test-smtp.mjs